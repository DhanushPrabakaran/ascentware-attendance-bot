const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { const emp = await prisma.employee.findMany(); console.log(emp); } main().finally(() => prisma.$disconnect());
